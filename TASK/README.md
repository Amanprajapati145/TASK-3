TAsk : fetch the data from api using axios and display in another component

obajective => fetch the data from an API using axios in Reactjs. passs the data to another component , and display it.      fetch the data in fetch data component and display it userlist component

task discription :

1. create a react application
2. install axios in your project
npm install axios

3. create two components

* Parent component(FetchData.jsx)
* child component (UserList.jsx)

4. in the parent component
